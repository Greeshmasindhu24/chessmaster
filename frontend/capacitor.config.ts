import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Greeshma.ChessMaster.Pro',
  appName: 'CHESSMASTER PRO',
  webDir: 'dist'
};

export default config;
